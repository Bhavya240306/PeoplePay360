# Python
__pycache__/
*.py[cod]
*$py.class
*.so

# Django
*.log
local_settings.py
# db.sqlite3 comes from settings.py's zero-setup fallback when DB_ENGINE
# isn't set to 'postgres' (loudly warned about there) — Postgres is the
# real target stack, this file should never be intentionally committed.
db.sqlite3
db.sqlite3-journal
media/
staticfiles/

# Env / secrets
.env
.env.*
!.env.example

# Virtual environments
venv/
env/
.venv/
ENV/

# Editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# React / Node (frontend)
node_modules/
build/
dist/
.env.local
.env.development.local
.env.production.local
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Test / coverage
.coverage
htmlcov/
.pytest_cache/